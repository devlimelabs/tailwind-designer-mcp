# Environment variables for NPM Package Manager MCP
# Currently no specific configuration is required

# Example: If you want to use a custom npm registry
# NPM_REGISTRY=https://registry.npmjs.org/

# Example: If you need to set a specific directory for operations
# WORKSPACE_DIR=/workspace